{
  "name": "@flipp-test/hosted-tiles",
  "private": true,
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "tslib": "^2.1.0"
  }
}
